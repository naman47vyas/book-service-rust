Rust comiples to a binary so just hit:
```
cargo build
```

and to run the project:

```
./target/debug/book_library
```

We have also added a python client that makes requests to this server to the server:

```
cd chaos-client
python3 main.py
```
